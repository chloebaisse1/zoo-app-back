CREATE TABLE CompteR(
    id INT PRIMARY KEY NOT NULL,
    nom VARCHAR(255),
    race VARCHAR(255),
    habitat VARCHAR(255),
    nourriture VARCHAR(255),
    quantitee VARCHAR(255),
    date DATETIME,
    commentaire TEXT,
)